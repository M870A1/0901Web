<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mybatis="http://mybatis.org/schema/mybatis-spring"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
                           http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring.xsd
                           http://www.springframework.org/schema/tx https://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- Root Context: 정의된 모든 Spring 빈들이 웹 애플리케이션의 모든 계층에서 사용될 수 있도록 -->
    <!-- 주로 서비스 계층, 데이터베이스 접근 계층 등 웹과 직접 관련 없는 빈들을 정의 -->

    <!-- @Repository, @Service, @Component 어노테이션이 붙은 클래스를 스캔하여 빈으로 등록합니다. -->
    <!-- Controller는 servlet-context.xml (spring-servlet.xml)에서 스캔하므로 제외할 수 있습니다. -->
    <context:component-scan base-package="org.zerock.yesyes2"/>

    <bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
        <property name="driverClassName" value="org.mariadb.jdbc.Driver" />
        <property name="jdbcUrl" value="jdbc:mariadb://localhost:3306/son"/>
        <property name="username" value="root"/>
        <property name="password" value="1234"/>
        <property name="dataSourceProperties">
            <props>
                <prop key="cachePrepStmts">true</prop>
                <prop key="prepStmtCacheSize">250</prop>
                <prop key="prepStmtCacheSqlLimit">2048</prop>
            </props>
        </property>
    </bean>
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource"
          destroy-method="close">
        <constructor-arg ref="hikariConfig"/>
    </bean>
    <!--  Mybatis 설정(SQL을 xml파일에 작성하여 코드와 SQL을 분리하여 개발하는 방식)-->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <!-- DB연결에 필요한 DataSource 설정 -->
        <property name="dataSource" ref="dataSource"/>
        <!-- mybatis에서 사용하는 xml파일의 위치 설정-->
        <property name="mapperLocations" value="classpath:/mappers/**/*.xml"/>
    </bean>
    <!-- xml파일과 연동할 java파일들의 위치 설정 -->
    <mybatis:scan base-package="org.zerock.yesyes2.mappers" />
    
    <!-- 트랜잭션 매니저 등록 -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- @Transactional 어노테이션 활성화 -->
    <tx:annotation-driven/>
</beans>